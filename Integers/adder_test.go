package Integers

import (
	"fmt"
	"testing"
)

func TestAdder(t *testing.T) {
	sum := Add(2, 2)
	expected := 4

	if sum != expected {
		t.Errorf("expected '%d' but got '%d'", expected, sum)
	}
}

func ExampleAdd() {
	sum := Add(1, 5)
	fmt.Println(sum)
	// Output: 6
}

// The comment is needed. Examples without comments are
//useful for demonstrating code that cannot run as unit tests
//(like test that access the network)
//while guaranteeing that the example at least compiles
